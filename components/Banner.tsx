import Image from "next/image";
import { IMAGES } from "@/constants/assetPaths";

const Banner = () => {
  return (
    <section className="container h-screen">
      <div className="flex justify-center items-center border-b border-white"></div>
    </section>
  );
};

export default Banner;
